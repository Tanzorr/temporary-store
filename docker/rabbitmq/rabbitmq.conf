# The default `guest` user may only authenticate from RabbitMQ's own loopback
# by default. Containers reach it over the compose network, not localhost, so
# without this the guest/guest credentials in .env.example never connect.
# Dev-only broker; see stack.md.
loopback_users.guest = false
