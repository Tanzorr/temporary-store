; Exceeds UPLOAD_MAX_SIZE_BYTES (10 MB) at the PHP layer. See AC-9 on TASK-001:
; a value below the app's limit surfaces as an empty request, not a 422.
upload_max_filesize = 12M
post_max_size = 12M
memory_limit = 256M
max_execution_time = 120
